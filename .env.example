# The public origin, without a trailing slash. Set this in the Vercel dashboard
# once a custom domain is attached; until then Vercel's own
# VERCEL_PROJECT_PRODUCTION_URL is used automatically and this can stay unset.
NEXT_PUBLIC_SITE_URL=https://mauricioraposo.com
