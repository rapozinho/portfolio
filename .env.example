# The public origin, no trailing slash. Set this in the Vercel dashboard
# (Project -> Settings -> Environment Variables, Production) once the domain
# is attached. Until then Vercel's own VERCEL_PROJECT_PRODUCTION_URL is used
# automatically and this can stay unset.
NEXT_PUBLIC_SITE_URL=https://mauricioraposo.dev
