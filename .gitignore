# Reference material — 221MB, including the source video. Kept locally, not versioned.
fotosreferenciablackwall/

# Loose screenshots dropped in the root during design review
*.png
!prototipo/**/*.png

# Node / build output, in case a Next.js app lands here later
node_modules/
.next/
out/
dist/
.vercel/
*.tsbuildinfo

# Python scratch
__pycache__/
*.pyc

# Renderer output (regenerate with: python render.py <tag>)
prototipo/frames/

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Originals live in public/ now, served as static files. Versioning both copies
# would put 21MB of duplicate PDFs in every clone.
/Certificados/
/Currículos/
/oq faco no trampo/

# Next.js build output and dependencies
/.next/
/node_modules/
next-env.d.ts
*.tsbuildinfo

# Local env (the example file is the one that is versioned)
.env
.env.local

# stray syntax-check extracts
pc*.js
proto_check.js
